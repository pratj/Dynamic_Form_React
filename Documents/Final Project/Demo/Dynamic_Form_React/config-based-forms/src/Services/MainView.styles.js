import styled from "@emotion/styled";

export const MainViewContainer= styled.div`
    
    width:${p=>p.isSidebarOpen ? '80%' : '95%'};
    position:;
    background: ${p=>p.isSidebarOpen ? '#e8e8e8' : '#e8e8e8'}
    
    
    `
